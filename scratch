db.create_all()
test_user=User(fname='John',lname='last',email='test@test.test', password='test')
db.session.add(test_user)
db.session.commit()

>>> user = User.query.first()
>>> user

new_photo = Photo(img='img',description='great',location='lock',gps_url='abc',popular_url='abc')
 db.session.add(new_photo)
 db.session.commit()

photos = Photo.query.all()
photos

rat=Rating(rating=3,user=test_user,photo=photos[0])
db.session.add(rat)
db.session.commit()
ratings=Rating.query.all()
print(ratings[0].rating)
print(ratings[0].photo)

fav_photo=Favorite_Photo(photo=photos[0],user=test_user)
db.session.add(fav_photo)
db.session.commit()

favs=Favorite_Photo.query.all()
print(favs[0].user)
print(favs[0].photo)


crud.get_user_by_lname("Doe")
crud.get_users_by_lname("Doe")


To restart database use

sudo service postgresql restart

"img": "./static/images/img1.jpg",
        "location": "Halebeedu, Karnataka, India",
        "date_taken":"2018-08-02",
        "description":"Halebidu is a small town located in Hassan District in Karnataka state. It is a hindu temple for god Shiva. It's famous for its architecture with intricate detials.It's architecture was destructed during mughals invasion during 12th century."